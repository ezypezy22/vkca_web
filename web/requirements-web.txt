# VK Contest Analyzer — Web mode dependencies
# Install with: pip install -r requirements-web.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pywebview>=5.0.0
python-multipart>=0.0.9   # required by FastAPI/Starlette to parse file uploads
